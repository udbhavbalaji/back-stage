const getFormattedJSON = (obj: Record<any, any> | Array<any>): string => {
  return "";
};

const getFormattedJSONTypes = (obj: Record<any, any> | Array<any>): string => {
  return "";
};

export { getFormattedJSONTypes, getFormattedJSON };
